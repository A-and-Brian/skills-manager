---
"skills-manager": minor
---

pr: #1

Group the Library by tag, source or agent, sort it by name, recent changes or update status, and filter by agent or update state. Unmanaged skills in an agent's global folder can now be deleted even when their content matches a library skill. Adds remote hosts: manage skills on another machine running Skills Manager over `ssh`, with no daemon, open port or stored credentials.
