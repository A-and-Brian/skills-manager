---
"skills-manager": minor
---

pr: #14

Add `skills-manager-cli serve --stdio`, which runs the app's commands on a remote machine over a single `ssh` connection. Both sides must run the same version.
